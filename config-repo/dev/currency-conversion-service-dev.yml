server: 
  port: 8101

spring: 
  jpa: 
    show-sql: true
  h2:
    console:
      enabled: true
  cloud:
    loadbalancer:
      ribbon:
        enabled: true

currency-exchange-service:
  ribbon:
    listOfServers: localhost:8071, localhost:8061